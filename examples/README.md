# Example IFC fixtures

- `tiny.min.ifc` — minimal hand-crafted IFC4 with a project, context, units,
  one wall, and one slab. ~15 entities. Used by integration tests.
- `tiny-v2.min.ifc` — same file with the wall renamed and a new column,
  demonstrating the three change kinds (rename, add, unchanged).
